node_modules/
dist/
.vite/

# Vendored prince-ui tarballs are committed so the branch builds reproducibly
# (npm publish is gated behind 2FA). Ignore any other vendor junk.
vendor/*
!vendor/prince-ui
vendor/prince-ui/*
!vendor/prince-ui/*.tgz
